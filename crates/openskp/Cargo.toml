[package]
name = "openskp"
description = "Clean-room reader for the SketchUp .skp binary format (SketchUp 2017)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

# The core library has ZERO runtime dependencies by design, so it drops
# cleanly into any build. Test-only deps live under dev-dependencies.

[dev-dependencies]
serde_json = "1"
